# Blackboard — 001 Fix retry logic

> Generated by relay at task creation.
> All sections are open to human and agent writes.
> Agents: read selectively by section heading — only load what your current step needs.

---

## Plan
<!-- Current working plan. Agent updates as it goes. Human can revise. -->

1. Read `lib/webhooks/retry.ts` and the surrounding webhook handler.
2. Add `Retry-After` parsing to the 429 branch — fall back to current
   backoff if absent.
3. Cap maximum delay at 5 minutes (sanity bound).
4. Emit a structured log line on retry exhaustion so we can dashboard it.
5. Tests: unit-test the parser; integration-test the full retry loop
   against a mock 429 stream.

---

## Notes
<!-- Free space. Drop anything here — questions, observations, links. -->

- Stripe's `Retry-After` can be either delta-seconds or HTTP-date. Plan
  step 2 needs to handle both formats.

---

## Findings
<!-- Agent: discovered facts, intermediate results, research outputs. -->

- The existing backoff is implemented as a switch on attempt count; it'll
  need to become a function of (attempt, headers).
- `lib/observability/metrics.ts` already exposes a `webhookRetryFailed`
  counter — reuse it instead of adding a new one.

---

## Blockers
<!-- Anything stalling progress. Human or agent can write. -->

(none)

---

## Decisions
<!-- Rationale for choices made. Survives across runs. -->
<!-- Format: [date] [actor] decision + reason -->

- [2026-04-25] [agent:claude1] cap at 5 minutes — Stripe's docs
  recommend not exceeding this for webhook retries.
- [2026-04-25] [human:marc] reuse `webhookRetryFailed` metric instead of
  adding a new one — keeps the observability surface tidy.
