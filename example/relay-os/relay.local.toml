# Local per-machine config. `relay.local.toml` is gitignored in a real repo;
# this copy ships with the example so the smoke test has something to read.

user = "marc"

[secrets]
# Populated from env vars at launch time using env:VAR_NAME refs.
