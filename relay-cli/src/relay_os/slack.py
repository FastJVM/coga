"""Slack feed — webhook-based posting for the shared channel.

Stub. Full implementation lands in ticket FJVM-1295.

Expected surface (tentative):

    def post(message: str, mention_user_id: str | None = None) -> bool
"""
