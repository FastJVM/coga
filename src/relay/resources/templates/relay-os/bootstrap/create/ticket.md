---
title: Author a new task
mode: interactive
skill: bootstrap/create
assignee: claude1
---

## Description

Persistent launch shim. Run with `relay launch --task bootstrap/create` to
start the `bootstrap/create` skill, which interviews the human and scaffolds
a new task.

This ticket is stateless. It has no status and acquires no lock — every
launch is a fresh authoring session. Don't edit the ticket itself except to
swap the `assignee` to whichever agent nickname you have configured in
`relay.toml`.
