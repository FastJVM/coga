---
title: Replace with a one-line human-readable title
status: ready
mode: interactive
owner: replace-with-human-name
assignee: replace-with-human-or-agent-nickname
# watchers:
#   - someone-else
# workflow:                 # frozen snapshot — `relay create --workflow <name>` writes this for you.
#   name: code/with-review
#   steps:
#     - name: implement
#       skill: infra/testing-conventions
#     - name: pr
#     - name: approve
#       skill: process/approve
#     - name: merge
# step: 1 (implement)       # only present when `workflow:` is set.
# contexts:
#   - email/payment-flow
---

## Description

What needs to happen and why. One paragraph is plenty for most tasks.
The agent reads the composed prompt, not this body — these sections exist
to help humans organize their thinking.

## Context

Task-specific knowledge that isn't a reusable skill or context file.
One-off details: where in the codebase, what to watch out for, what not
to touch.
