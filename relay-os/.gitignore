# >>> relay-managed >>>
# Managed by `relay init [--update]`. Don't edit between these markers —
# they will be overwritten on update. Add your own ignore rules below the
# end marker.
# Machine-local config — paths and secrets. Never commit.
relay.local.toml

# Vendored CLI — refreshed by `relay init --update` from upstream.
.relay/

# Single relay-vendored umbrella. Everything inside is upstream-managed and
# overwritten on every `relay init --update` — your edits will be lost.
# User content goes outside this dir (skills/<your-ns>/, contexts/<your-ns>/).
bootstrap/

# Back-compat symlinks pointing into bootstrap/ above. Recreated by
# `relay init [--update]`; safe to delete locally — they get regenerated.
skills/bootstrap
skills/retro
skills/relay
contexts/relay/architecture
contexts/relay/principles
contexts/relay/cli

# Per-domain scaffolding templates (bare `_template`, `_template.md`).
# Refreshed wholesale on update.
**/_template/
**/_template.md
# <<< relay-managed <<<
