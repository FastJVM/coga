The blackboard is a notepad to be written to often as the human and agent works through a task.

## Dream Skill: validate-drift

Generated: 2026-05-22T22:56:28+00:00
Command: `/home/n/Code/relay/relay-os/.relay/.venv/bin/python -m relay.validate --json --fix`
Task: `dream-w21-validate-drift`

Result: 12 issue(s): 0 direct fix, 0 PR proposal, 12 human-needed.

### Human Needed

- `add-always-on-context-tier`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `add-browser-skill-for-automating-tasks-click-test`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `autotrigger-ticket-type`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `install-init-skills-via-skill-downloader`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `launch-tasks-in-container-or-vm`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `pass-secrets-to-skills-with-per-skill-scope`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `plan-second-wave-dream-workers`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `recurring-task-for-relay-dev-udpate`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `rewrite-slack-messages`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `stream-agent-progress-in-auto-mode-and-recurring-l`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `token-budget-aware-idle-execution-of-low-priority`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.
- `use-slack-as-a-sync-channel-for-tickets`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: Unknown validator issue kind. Ask a human before changing repo state.

## Retro

status: processed
skill: retro/done-ticket
result: no-new-durable-knowledge
title: No new durable knowledge for dream-w21-validate-drift
