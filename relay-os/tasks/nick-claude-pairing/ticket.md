---
title: Standing pairing session — Nick + claude1
status: draft
mode: interactive
owner: nick
assignee: claude1
contexts:
  - relay/architecture
  - relay/principles
  - relay/codebase
  - relay/current-direction
  - relay/project-stage
---

## Description

Standing ticket. Not a deliverable — this exists so `relay launch
nick-claude-pairing` drops the agent into a fully-composed relay-aware
session without binding to any specific work. Nick directs in chat from
there.

No workflow (no `step:` semantics — `relay bump` doesn't apply). Don't
move to `done` — leave in `draft` (or `active` mid-session) indefinitely.
If the standing-collaboration shape changes, edit this body.

Replaces "open `claude` in the repo and re-explain the project every
time." A real meta-launcher (one shim any user/agent pair can re-use) is
the longer-term shape — file separately if/when needed.
