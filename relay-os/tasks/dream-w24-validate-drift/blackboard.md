The blackboard is a notepad to be written to often as the human and agent works through a task.

## Dream Skill: validate-drift

Generated: 2026-06-11T18:22:43+00:00
Command: `/home/n/Code/relay/relay-os/.relay/.venv/bin/python -m relay.validate --json --fix`
Task: `dream-w24-validate-drift`

Result: 67 issue(s): 0 direct fix, 0 PR proposal, 67 human-needed.

### Human Needed

- `acceptance-criteria`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `acceptance-criteria`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `add-a-first-class-relay-config-directory-for-machi`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `add-browser-skill-for-automating-tasks-click-test`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `add-killer-demo`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `add-subproject`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `authentication-system`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `autocleanup-worktree-branche`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `autoclose-merged`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `autoroute-agent-based-on-remaining-usage`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `autotrigger-ticket-type`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `clean-uncommitted-work`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `clean-uncommitted-work`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `compose-strips-skill-md-and-context-frontmatter-be`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `drift-status-still-calls-auto-bump-merged-after-mo`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `enforce-a-prompt-token-budget-in-compose`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `establish-marketing-area-inside-relay-os`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `fail-loud-when-an-env-indirected-secret-is-missing`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `file-locking-for-concurrent-task-mutation`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `generic-lib-to-use-e-g-patent-models`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `identify-blocking-issues`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `identify-blocking-issues`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `improve-prompt-for-relay-launch`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `improve-prompt-for-relay-ticket`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `improve-readme-and-doc`: `unfrozen-workflow` (warn) - workflow 'code/with-review' is not a frozen dict — likely a hand-authored ticket awaiting first launch
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `init-questions`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `init-questions`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `issue-inbox-slack`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `issue-inbox-slack`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `launch-relay-product-launch-comms`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `launch-tasks-in-container-or-vm`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `log-timestamps-need-seconds-and-timezone-for-unamb`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `manage-security-and-pii`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `model-selector`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `pass-secrets-to-skills-with-per-skill-scope`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `recover-recurring-runs-orphaned-when-the-superviso`: `stuck-in-progress` (warn) - in_progress but idle for 142.7h
  Remediation: Ask the owner whether the task should be relaunched, panicked, paused, or bumped. The skill should not change lifecycle state silently.
- `recurring-task-not-instantiated`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `relay-cli-shipping`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `relay-crm`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `relay-crm`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `relay-discord`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `relay-discord`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `relay-forces-https`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `relay-forces-https`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `relay-model-selection`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `relay-model-selection`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `relay-project-command`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `relay-project-command`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `relay-uninstall`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `relay-uninstall`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `remote-default-origin`: `unknown-assignee` (warn) - assignee 'nick' is neither a known agent type nor one of this ticket's role-field values
  Remediation: Needs an owner decision because the correction changes task routing, workflow state, or who is expected to act next.
- `remote-default-origin`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `represent-autonomy-tier-in-ticket-mode-field`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `script-mode-to-activate`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `session-done-sentinel-from-mark-done-bump-leaks-in`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `session-done-sentinel-leaks-and-agent-stops-respon`: `stuck-in-progress` (warn) - in_progress but idle for 122.0h
  Remediation: Ask the owner whether the task should be relaunched, panicked, paused, or bumped. The skill should not change lifecycle state silently.
- `simplify-repl-supervisor-drop-pty-for-sentinel-pol`: `stuck-in-progress` (warn) - in_progress but idle for 110.6h
  Remediation: Ask the owner whether the task should be relaunched, panicked, paused, or bumped. The skill should not change lifecycle state silently.
- `slack-post-ignores-http-response-so-bad-webhook-fa`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `split-context-to-doc-user-accessible-and-editable`: `missing-step` (error) - `workflow:` is set but `step:` is missing
  Remediation: The ticket's current `step:` is not in its frozen workflow. Lifecycle correction is human-only; ask the owner to relaunch, rewind, or hand-edit the step.
- `supervisor-liveness-watchdog-for-agents-that-never`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `token-budget-aware-idle-execution-of-low-priority`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `uncommitted-stuff-not-handled`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `update-all-doesn-t-copy-workflow-correctly-to-atta`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `use-worktree-when-starting-a-dev-task`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `validate-skill-md-frontmatter-conformance-not-just`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `why-ai-asks-me-to-bump-instead-of-doing-it`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
- `write-prompt-file-second-resolution-timestamps-col`: `missing-workflow` (warn) - draft has no `workflow:` — it can't be activated (`relay mark active` refuses a workflow-less ticket). Set `workflow: <name>` or run `relay ticket <slug>`.
  Remediation: A draft without a workflow is a design state, not drift. The owner picks a workflow via `relay ticket <slug>` or by editing frontmatter; Dream does not pick workflows for humans.
