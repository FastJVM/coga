# Relay relaunch chain — test artifact

## 1. draft (claude)

This document is throwaway scaffolding for the auto-relaunch chain probe. Each
step appends its own section; the real deliverable is the relaunch observations
in `blackboard.md`, not this prose.

## 2. expand (claude)

The `expand` step was reached by auto-relaunch — the same supervisor process
that ran `draft` respawned this REPL with no human in between. This section
exists only to give the step something to write; the boundary observation lives
in the blackboard.

## 3. peer-pass (codex)

The `peer-pass` step was reached by auto-relaunch across an agent rotation:
the same supervisor process moved from the claude `expand` step to this codex
step. This section is still throwaway scaffolding; the useful evidence is in
the blackboard entry for this step.
